{% extends 'base.html.twig' %}

{% block title %}{{ title }} | {{ parent() }}{% endblock %}

{% block body %}
    <h2>Wish List</h2>
    <ul>
        {% for w in wishes %}
            <li>
                <a href="{{ path('wish_detail',{id:w.id}) }}">
                {{ w.title }}
                </a>
            </li>
        {% endfor %}
    </ul>

<a href="{{ path('wish_create') }}">Create a new Wish</a>
  <!--  <a href="{#{ path('wish_delete') }  #}">Create a new Wish</a> -->

{% endblock %}
